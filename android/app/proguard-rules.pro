# Add project-specific ProGuard rules here.
# Keep libsignal / WebRTC JNI-bound classes once those libraries are added in Phase 2:
# -keep class org.signal.** { *; }
# -keep class org.webrtc.** { *; }
